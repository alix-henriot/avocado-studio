import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'source.unsplash.com', 'api.unsplash.com', 'picsum.photos', 'app.requestly.io']
  }
};

export default nextConfig;
